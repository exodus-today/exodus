@model long
@{
    var itemID = Model;
    var UserID = Exodus.Global.Global.CurrentUser.UserID;
    var lang = Exodus.Global.Global.Language;
}
<div class="ex-panel__content" data-react="@this.Url.Action("FriendObligations", "Intentions", new {UserID=UserID,  itemID = itemID})">
    @Exodus.Global.Localisation.Get("ObligationsInMyFavour", lang)
</div>
